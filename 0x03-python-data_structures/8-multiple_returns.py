#!/usr/bin/python3
def multiple_returns(sentence):
    if not sentence:
        return False
    else:
        for i in range(len(sentence)):
            a = sentence[0]
            b = len(sentence)
        return b, a
